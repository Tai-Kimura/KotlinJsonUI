<?xml version="1.0" encoding="utf-8"?>
<!-- Probe activities for the IME-follow arms: the consumer's window shape
     (adjustNothing + edge-to-edge, the reporting face's MainActivity) and the
     framework-resize shape as its control. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name="com.kotlinjsonui.components.AdjustNothingEdgeToEdgeActivity"
            android:windowSoftInputMode="adjustNothing"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name="com.kotlinjsonui.components.AdjustResizeActivity"
            android:windowSoftInputMode="adjustResize"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
    </application>
</manifest>
