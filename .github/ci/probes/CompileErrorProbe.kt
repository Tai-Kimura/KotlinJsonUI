// Positive control for the CI compile step — copied into sample-app's main
// sources by ci.yml and removed again; it does not compile, on purpose.
package com.kotlinjsonui.ciprobe

internal val ciCompileErrorProbe: Int = "not an int"
