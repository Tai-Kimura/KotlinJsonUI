// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang kotlin

package com.kotlinjsonui.dynamic.generated

/** Typed attribute extraction for the `Button` component.
 * Shared attributes are available via [common].
 * Overrides the common definition of: `highlightBackground`, `tapBackground` (use the property on this class).
 */
data class ButtonAttributes(
    /** Attributes shared across all components. */
    val common: CommonAttributes,
    /** HTML button type */
    val buttonType: AttrEnum<ButtonType>? = null,
    /** Button configuration (iOS 15+) */
    val config: Map<String, Any?>? = null,
    /** Font color when disabled - hex string or color name from colors.json (binding supported) */
    val disabledFontColor: AttrValue<String>? = null,
    /** Font name or 'bold' */
    val font: String? = null,
    /** Font color - hex string or color name from colors.json (binding supported) */
    val fontColor: AttrValue<String>? = null,
    /** Font size */
    val fontSize: Double? = null,
    /** Font weight (e.g., 'bold', 'semibold', '500', 600, or binding) [accepts: string | number] */
    val fontWeight: AttrValue<Any>? = null,
    /** Background when highlighted - hex string or color name from colors.json */
    val highlightBackground: String? = null,
    /** Highlight color - hex string or color name from colors.json (binding supported) [aliases: hilightColor] */
    val highlightColor: AttrValue<String>? = null,
    /** Highlight color - hex string or color name from colors.json (typo alias) */
    val hilightColor: String? = null,
    /** Button image */
    val image: String? = null,
    /** Background when tapped - hex string or color name from colors.json */
    val tapBackground: String? = null,
    /** Button text (can be data binding, supports interpolation) */
    val text: AttrValue<String>? = null,
    /** Text alignment */
    val textAlign: AttrEnum<TextAlign>? = null,
) {
    enum class ButtonType(val json: String) {
        BUTTON("button"),
        SUBMIT("submit"),
        RESET("reset");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): ButtonType? = when (raw.lowercase()) {
                "button" -> BUTTON
                "submit" -> SUBMIT
                "reset" -> RESET
                else -> null
            }
        }
    }

    enum class TextAlign(val json: String) {
        LEFT("Left"),
        CENTER("Center"),
        RIGHT("Right");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): TextAlign? = when (raw.lowercase()) {
                "left" -> LEFT
                "center" -> CENTER
                "right" -> RIGHT
                else -> null
            }
        }
    }

    companion object {
        /**
         * Canonical attribute names declared for this component, including
         * the shared `common` set (public metadata contract).
         */
        val declaredAttributes: Set<String> = CommonAttributes.declaredAttributes + setOf(
            "buttonType",
            "config",
            "disabledFontColor",
            "font",
            "fontColor",
            "fontSize",
            "fontWeight",
            "highlightBackground",
            "highlightColor",
            "hilightColor",
            "image",
            "tapBackground",
            "text",
            "textAlign",
        )

        /**
         * Alias spelling → canonical attribute name (merged with common).
         * Alias spellings that are also declared attributes keep
         * their own entry and are not redirected.
         */
        val aliasMap: Map<String, String> = emptyMap()

        /** True when `key` is a declared canonical name or alias spelling. */
        fun isDeclared(key: String): Boolean =
            key in declaredAttributes || key in aliasMap

        /**
         * Pass `canonicalOnly = true` for L1-normalized input —
         * alias fallback is then disabled.
         */
        fun parse(json: Map<String, Any?>, canonicalOnly: Boolean = false): ButtonAttributes = ButtonAttributes(
            common = CommonAttributes.parse(json, canonicalOnly),
            buttonType = parseButtonType(AttrCoerce.lookup(json, "buttonType")),
            config = AttrCoerce.obj(AttrCoerce.lookup(json, "config")),
            disabledFontColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "disabledFontColor")) { AttrCoerce.string(it) },
            font = AttrCoerce.string(AttrCoerce.lookup(json, "font")),
            fontColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "fontColor")) { AttrCoerce.string(it) },
            fontSize = AttrCoerce.number(AttrCoerce.lookup(json, "fontSize")),
            fontWeight = AttrCoerce.attrValue(AttrCoerce.lookup(json, "fontWeight")) { it },
            highlightBackground = AttrCoerce.string(AttrCoerce.lookup(json, "highlightBackground")),
            highlightColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "highlightColor", listOf("hilightColor"), canonicalOnly)) { AttrCoerce.string(it) },
            hilightColor = AttrCoerce.string(AttrCoerce.lookup(json, "hilightColor")),
            image = AttrCoerce.string(AttrCoerce.lookup(json, "image")),
            tapBackground = AttrCoerce.string(AttrCoerce.lookup(json, "tapBackground")),
            text = AttrCoerce.attrValue(AttrCoerce.lookup(json, "text")) { AttrCoerce.string(it) },
            textAlign = parseTextAlign(AttrCoerce.lookup(json, "textAlign")),
        )

        private fun parseButtonType(raw: Any?): AttrEnum<ButtonType>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                ButtonType.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("Button.buttonType: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseTextAlign(raw: Any?): AttrEnum<TextAlign>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                TextAlign.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("Button.textAlign: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }
    }
}
