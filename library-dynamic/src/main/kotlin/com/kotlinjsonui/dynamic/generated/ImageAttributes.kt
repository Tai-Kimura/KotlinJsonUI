// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang kotlin

package com.kotlinjsonui.dynamic.generated

/** Typed attribute extraction for the `Image` component.
 * Shared attributes are available via [common].
 * Overrides the common definition of: `canTap` (use the property on this class).
 */
data class ImageAttributes(
    /** Attributes shared across all components. */
    val common: CommonAttributes,
    /** Alt text for accessibility */
    val alt: String? = null,
    /** Enable tap gesture independent of onClick. */
    val canTap: Boolean? = null,
    /** Content mode (binding supported) */
    val contentMode: AttrValue<AttrEnum<ContentMode>>? = null,
    /** Fallback image on load error. */
    val errorImage: String? = null,
    /** Image when highlighted (binding supported) */
    val highlightSrc: AttrValue<String>? = null,
    /** Highlight image source name (binding supported) */
    val highlightSrcName: AttrValue<String>? = null,
    /** Loading behavior */
    val loading: AttrEnum<Loading>? = null,
    /** Placeholder shown while the main image loads. */
    val loadingImage: String? = null,
    /** Pinch-zoom upper bound. */
    val maxZoom: Double? = null,
    /** Pinch-zoom lower bound. */
    val minZoom: Double? = null,
    /** Rendering mode */
    val renderingMode: AttrEnum<RenderingMode>? = null,
    /** Image source name (can be data binding) */
    val src: AttrValue<String>? = null,
    /** Image source name (alias, can be data binding) */
    val srcName: AttrValue<String>? = null,
    /** Interpret `src` as an SF Symbol name. */
    val systemIcon: Boolean? = null,
) {
    enum class ContentMode(val json: String) {
        FIT("fit"),
        FILL("fill"),
        CENTER("center"),
        TOP("top"),
        BOTTOM("bottom"),
        LEFT("left"),
        RIGHT("right"),
        ASPECT_FILL("AspectFill"),
        ASPECT_FIT("AspectFit"),
        SCALE_TO_FILL("ScaleToFill");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): ContentMode? = when (raw.lowercase()) {
                "fit" -> FIT
                "fill" -> FILL
                "center" -> CENTER
                "top" -> TOP
                "bottom" -> BOTTOM
                "left" -> LEFT
                "right" -> RIGHT
                "aspectfill" -> ASPECT_FILL
                "aspectfit" -> ASPECT_FIT
                "scaletofill" -> SCALE_TO_FILL
                else -> null
            }
        }
    }

    enum class Loading(val json: String) {
        LAZY("lazy"),
        EAGER("eager");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): Loading? = when (raw.lowercase()) {
                "lazy" -> LAZY
                "eager" -> EAGER
                else -> null
            }
        }
    }

    enum class RenderingMode(val json: String) {
        ORIGINAL("original"),
        TEMPLATE("template");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): RenderingMode? = when (raw.lowercase()) {
                "original" -> ORIGINAL
                "template" -> TEMPLATE
                else -> null
            }
        }
    }

    companion object {
        /**
         * Canonical attribute names declared for this component, including
         * the shared `common` set (public metadata contract).
         */
        val declaredAttributes: Set<String> = CommonAttributes.declaredAttributes + setOf(
            "alt",
            "canTap",
            "contentMode",
            "errorImage",
            "highlightSrc",
            "highlightSrcName",
            "loading",
            "loadingImage",
            "maxZoom",
            "minZoom",
            "renderingMode",
            "src",
            "srcName",
            "systemIcon",
        )

        /**
         * Alias spelling → canonical attribute name (merged with common).
         * Alias spellings that are also declared attributes keep
         * their own entry and are not redirected.
         */
        val aliasMap: Map<String, String> = emptyMap()

        /** True when `key` is a declared canonical name or alias spelling. */
        fun isDeclared(key: String): Boolean =
            key in declaredAttributes || key in aliasMap

        /**
         * Pass `canonicalOnly = true` for L1-normalized input —
         * alias fallback is then disabled.
         */
        fun parse(json: Map<String, Any?>, canonicalOnly: Boolean = false): ImageAttributes = ImageAttributes(
            common = CommonAttributes.parse(json, canonicalOnly),
            alt = AttrCoerce.string(AttrCoerce.lookup(json, "alt")),
            canTap = AttrCoerce.boolean(AttrCoerce.lookup(json, "canTap")),
            contentMode = AttrCoerce.attrValue(AttrCoerce.lookup(json, "contentMode")) { parseContentMode(it) },
            errorImage = AttrCoerce.string(AttrCoerce.lookup(json, "errorImage")),
            highlightSrc = AttrCoerce.attrValue(AttrCoerce.lookup(json, "highlightSrc")) { AttrCoerce.string(it) },
            highlightSrcName = AttrCoerce.attrValue(AttrCoerce.lookup(json, "highlightSrcName")) { AttrCoerce.string(it) },
            loading = parseLoading(AttrCoerce.lookup(json, "loading")),
            loadingImage = AttrCoerce.string(AttrCoerce.lookup(json, "loadingImage")),
            maxZoom = AttrCoerce.number(AttrCoerce.lookup(json, "maxZoom")),
            minZoom = AttrCoerce.number(AttrCoerce.lookup(json, "minZoom")),
            renderingMode = parseRenderingMode(AttrCoerce.lookup(json, "renderingMode")),
            src = AttrCoerce.attrValue(AttrCoerce.lookup(json, "src")) { AttrCoerce.string(it) },
            srcName = AttrCoerce.attrValue(AttrCoerce.lookup(json, "srcName")) { AttrCoerce.string(it) },
            systemIcon = AttrCoerce.boolean(AttrCoerce.lookup(json, "systemIcon")),
        )

        private fun parseContentMode(raw: Any?): AttrEnum<ContentMode>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                ContentMode.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("Image.contentMode: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseLoading(raw: Any?): AttrEnum<Loading>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                Loading.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("Image.loading: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseRenderingMode(raw: Any?): AttrEnum<RenderingMode>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                RenderingMode.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("Image.renderingMode: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }
    }
}
