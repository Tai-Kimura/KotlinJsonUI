// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang kotlin

package com.kotlinjsonui.dynamic.generated

/** Typed attribute extraction for the `ScrollView` component.
 * Shared attributes are available via [common].
 */
data class ScrollViewAttributes(
    /** Attributes shared across all components. */
    val common: CommonAttributes,
    /** Enable bounce effect */
    val bounces: Boolean? = null,
    /** Child component(s) */
    val child: List<Any?>? = null,
    /** Child components */
    val children: List<Any?>? = null,
    /** Content inset adjustment */
    val contentInsetAdjustmentBehavior: AttrEnum<ContentInsetAdjustmentBehavior>? = null,
    /** Initial offset [x, y] */
    val contentOffset: List<Any?>? = null,
    /** Content size [width, height] */
    val contentSize: List<Any?>? = null,
    /** Deceleration rate */
    val decelerationRate: String? = null,
    /** Scroll indicator style */
    val indicatorStyle: String? = null,
    /** Enable keyboard avoidance */
    val keyboardAvoidance: Boolean? = null,
    /** How scrolling dismisses the soft keyboard. Default 'none': scrolling never dismisses (keyboard stays). 'interactive': dragging down over the keyboard dismisses it interactively. 'onDrag': any scroll dismisses immediately. Value names follow UIKit UIScrollView.keyboardDismissMode; SwiftUI mode maps to scrollDismissesKeyboard(.never/.interactively/.immediately). iOS-effective; other platforms currently ignore it. */
    val keyboardDismissMode: AttrEnum<KeyboardDismissMode>? = null,
    /** Maximum zoom scale (binding supported) */
    val maxZoom: AttrValue<Double>? = null,
    /** Minimum zoom scale (binding supported) */
    val minZoom: AttrValue<Double>? = null,
    /** Scroll orientation */
    val orientation: AttrEnum<Orientation>? = null,
    /** Enable paging */
    val paging: Boolean? = null,
    /** Scroll behavior animation */
    val scrollBehavior: AttrEnum<ScrollBehavior>? = null,
    /** Enable scrolling (binding supported) */
    val scrollEnabled: AttrValue<Boolean>? = null,
    /** Where the scroll happens. 'inner' (default, iOS/Android-style): the ScrollView div itself gets overflow-y-auto and scrolls internally — requires a bounded-height ancestor on web to actually activate. 'window' (web-only escape hatch): skip the overflow class so the page/window scrolls naturally; fixes position:sticky descendants that would otherwise be anchored to a never-scrolling inner container. iOS/Android ignore this attribute (UIScrollView always scrolls internally). */
    val scrollMode: AttrEnum<ScrollMode>? = null,
    /** Scroll to top on status bar tap */
    val scrollsToTop: Boolean? = null,
    /** Show horizontal indicator */
    val showsHorizontalScrollIndicator: Boolean? = null,
    /** Show vertical indicator */
    val showsVerticalScrollIndicator: Boolean? = null,
) {
    enum class ContentInsetAdjustmentBehavior(val json: String) {
        NEVER("never"),
        ALWAYS("always"),
        AUTOMATIC("automatic"),
        SCROLLABLE_AXES("scrollableAxes");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): ContentInsetAdjustmentBehavior? = when (raw.lowercase()) {
                "never" -> NEVER
                "always" -> ALWAYS
                "automatic" -> AUTOMATIC
                "scrollableaxes" -> SCROLLABLE_AXES
                else -> null
            }
        }
    }

    enum class KeyboardDismissMode(val json: String) {
        NONE("none"),
        ON_DRAG("onDrag"),
        INTERACTIVE("interactive");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): KeyboardDismissMode? = when (raw.lowercase()) {
                "none" -> NONE
                "ondrag" -> ON_DRAG
                "interactive" -> INTERACTIVE
                else -> null
            }
        }
    }

    enum class Orientation(val json: String) {
        HORIZONTAL("horizontal"),
        VERTICAL("vertical");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): Orientation? = when (raw.lowercase()) {
                "horizontal" -> HORIZONTAL
                "vertical" -> VERTICAL
                else -> null
            }
        }
    }

    enum class ScrollBehavior(val json: String) {
        AUTO("auto"),
        SMOOTH("smooth");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): ScrollBehavior? = when (raw.lowercase()) {
                "auto" -> AUTO
                "smooth" -> SMOOTH
                else -> null
            }
        }
    }

    enum class ScrollMode(val json: String) {
        INNER("inner"),
        WINDOW("window");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): ScrollMode? = when (raw.lowercase()) {
                "inner" -> INNER
                "window" -> WINDOW
                else -> null
            }
        }
    }

    companion object {
        /**
         * Canonical attribute names declared for this component, including
         * the shared `common` set (public metadata contract).
         */
        val declaredAttributes: Set<String> = CommonAttributes.declaredAttributes + setOf(
            "bounces",
            "child",
            "children",
            "contentInsetAdjustmentBehavior",
            "contentOffset",
            "contentSize",
            "decelerationRate",
            "indicatorStyle",
            "keyboardAvoidance",
            "keyboardDismissMode",
            "maxZoom",
            "minZoom",
            "orientation",
            "paging",
            "scrollBehavior",
            "scrollEnabled",
            "scrollMode",
            "scrollsToTop",
            "showsHorizontalScrollIndicator",
            "showsVerticalScrollIndicator",
        )

        /**
         * Alias spelling → canonical attribute name (merged with common).
         * Alias spellings that are also declared attributes keep
         * their own entry and are not redirected.
         */
        val aliasMap: Map<String, String> = emptyMap()

        /** True when `key` is a declared canonical name or alias spelling. */
        fun isDeclared(key: String): Boolean =
            key in declaredAttributes || key in aliasMap

        /**
         * Pass `canonicalOnly = true` for L1-normalized input —
         * alias fallback is then disabled.
         */
        fun parse(json: Map<String, Any?>, canonicalOnly: Boolean = false): ScrollViewAttributes = ScrollViewAttributes(
            common = CommonAttributes.parse(json, canonicalOnly),
            bounces = AttrCoerce.boolean(AttrCoerce.lookup(json, "bounces")),
            child = AttrCoerce.array(AttrCoerce.lookup(json, "child")),
            children = AttrCoerce.array(AttrCoerce.lookup(json, "children")),
            contentInsetAdjustmentBehavior = parseContentInsetAdjustmentBehavior(AttrCoerce.lookup(json, "contentInsetAdjustmentBehavior")),
            contentOffset = AttrCoerce.array(AttrCoerce.lookup(json, "contentOffset")),
            contentSize = AttrCoerce.array(AttrCoerce.lookup(json, "contentSize")),
            decelerationRate = AttrCoerce.string(AttrCoerce.lookup(json, "decelerationRate")),
            indicatorStyle = AttrCoerce.string(AttrCoerce.lookup(json, "indicatorStyle")),
            keyboardAvoidance = AttrCoerce.boolean(AttrCoerce.lookup(json, "keyboardAvoidance")),
            keyboardDismissMode = parseKeyboardDismissMode(AttrCoerce.lookup(json, "keyboardDismissMode")),
            maxZoom = AttrCoerce.attrValue(AttrCoerce.lookup(json, "maxZoom")) { AttrCoerce.number(it) },
            minZoom = AttrCoerce.attrValue(AttrCoerce.lookup(json, "minZoom")) { AttrCoerce.number(it) },
            orientation = parseOrientation(AttrCoerce.lookup(json, "orientation")),
            paging = AttrCoerce.boolean(AttrCoerce.lookup(json, "paging")),
            scrollBehavior = parseScrollBehavior(AttrCoerce.lookup(json, "scrollBehavior")),
            scrollEnabled = AttrCoerce.attrValue(AttrCoerce.lookup(json, "scrollEnabled")) { AttrCoerce.boolean(it) },
            scrollMode = parseScrollMode(AttrCoerce.lookup(json, "scrollMode")),
            scrollsToTop = AttrCoerce.boolean(AttrCoerce.lookup(json, "scrollsToTop")),
            showsHorizontalScrollIndicator = AttrCoerce.boolean(AttrCoerce.lookup(json, "showsHorizontalScrollIndicator")),
            showsVerticalScrollIndicator = AttrCoerce.boolean(AttrCoerce.lookup(json, "showsVerticalScrollIndicator")),
        )

        private fun parseContentInsetAdjustmentBehavior(raw: Any?): AttrEnum<ContentInsetAdjustmentBehavior>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                ContentInsetAdjustmentBehavior.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("ScrollView.contentInsetAdjustmentBehavior: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseKeyboardDismissMode(raw: Any?): AttrEnum<KeyboardDismissMode>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                KeyboardDismissMode.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("ScrollView.keyboardDismissMode: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseOrientation(raw: Any?): AttrEnum<Orientation>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                Orientation.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("ScrollView.orientation: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseScrollBehavior(raw: Any?): AttrEnum<ScrollBehavior>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                ScrollBehavior.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("ScrollView.scrollBehavior: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseScrollMode(raw: Any?): AttrEnum<ScrollMode>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                ScrollMode.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("ScrollView.scrollMode: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }
    }
}
