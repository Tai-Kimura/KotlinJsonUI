// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang kotlin

package com.kotlinjsonui.dynamic.generated

/** Typed attribute extraction for the `Progress` component.
 * Shared attributes are available via [common].
 * Overrides the common definition of: `tintColor` (use the property on this class).
 */
data class ProgressAttributes(
    /** Attributes shared across all components. */
    val common: CommonAttributes,
    /** ActivityIndicator color, distinct from tintColor for historical reasons. */
    val color: String? = null,
    /** ActivityIndicator: auto-hide when stopped. */
    val hidesWhenStopped: Boolean? = null,
    /** ActivityIndicator size style. */
    val indicatorStyle: AttrEnum<IndicatorStyle>? = null,
    /** Progress value (0-1, can be binding) */
    val progress: AttrValue<Double>? = null,
    /** Custom progress image */
    val progressImage: String? = null,
    /** Progress portion color - hex string or color name from colors.json (binding supported) */
    val progressTintColor: AttrValue<String>? = null,
    /** Progress view style: default, bar */
    val progressViewStyle: String? = null,
    /** Tint color - hex string or color name from colors.json */
    val tintColor: String? = null,
    /** Custom track image */
    val trackImage: String? = null,
    /** Track portion color - hex string or color name from colors.json (binding supported) */
    val trackTintColor: AttrValue<String>? = null,
) {
    enum class IndicatorStyle(val json: String) {
        MEDIUM("medium"),
        LARGE("large");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): IndicatorStyle? = when (raw.lowercase()) {
                "medium" -> MEDIUM
                "large" -> LARGE
                else -> null
            }
        }
    }

    companion object {
        /**
         * Canonical attribute names declared for this component, including
         * the shared `common` set (public metadata contract).
         */
        val declaredAttributes: Set<String> = CommonAttributes.declaredAttributes + setOf(
            "color",
            "hidesWhenStopped",
            "indicatorStyle",
            "progress",
            "progressImage",
            "progressTintColor",
            "progressViewStyle",
            "tintColor",
            "trackImage",
            "trackTintColor",
        )

        /**
         * Alias spelling → canonical attribute name (merged with common).
         * Alias spellings that are also declared attributes keep
         * their own entry and are not redirected.
         */
        val aliasMap: Map<String, String> = emptyMap()

        /** True when `key` is a declared canonical name or alias spelling. */
        fun isDeclared(key: String): Boolean =
            key in declaredAttributes || key in aliasMap

        /**
         * Pass `canonicalOnly = true` for L1-normalized input —
         * alias fallback is then disabled.
         */
        fun parse(json: Map<String, Any?>, canonicalOnly: Boolean = false): ProgressAttributes = ProgressAttributes(
            common = CommonAttributes.parse(json, canonicalOnly),
            color = AttrCoerce.string(AttrCoerce.lookup(json, "color")),
            hidesWhenStopped = AttrCoerce.boolean(AttrCoerce.lookup(json, "hidesWhenStopped")),
            indicatorStyle = parseIndicatorStyle(AttrCoerce.lookup(json, "indicatorStyle")),
            progress = AttrCoerce.attrValue(AttrCoerce.lookup(json, "progress")) { AttrCoerce.number(it) },
            progressImage = AttrCoerce.string(AttrCoerce.lookup(json, "progressImage")),
            progressTintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "progressTintColor")) { AttrCoerce.string(it) },
            progressViewStyle = AttrCoerce.string(AttrCoerce.lookup(json, "progressViewStyle")),
            tintColor = AttrCoerce.string(AttrCoerce.lookup(json, "tintColor")),
            trackImage = AttrCoerce.string(AttrCoerce.lookup(json, "trackImage")),
            trackTintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "trackTintColor")) { AttrCoerce.string(it) },
        )

        private fun parseIndicatorStyle(raw: Any?): AttrEnum<IndicatorStyle>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                IndicatorStyle.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("Progress.indicatorStyle: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }
    }
}
