// @generated by `jui generate attr-bindings` — DO NOT EDIT BY HAND.
// Source of truth: shared/core/attribute_definitions.json
// Regenerate: jui generate attr-bindings --lang kotlin

package com.kotlinjsonui.dynamic.generated

/** Typed attribute extraction for the `SelectBox` component.
 * Shared attributes are available via [common].
 */
data class SelectBoxAttributes(
    /** Attributes shared across all components. */
    val common: CommonAttributes,
    /** Show back button */
    val canBack: Boolean? = null,
    /** Caret/arrow styling attributes */
    val caretAttributes: Map<String, Any?>? = null,
    /** Color scheme for native date picker icon visibility (set 'dark' for dark backgrounds) */
    val colorScheme: AttrEnum<ColorScheme>? = null,
    /** Date picker mode */
    val datePickerMode: AttrEnum<DatePickerMode>? = null,
    /** Date picker style */
    val datePickerStyle: AttrEnum<DatePickerStyle>? = null,
    /** Date string format */
    val dateStringFormat: String? = null,
    /** Divider styling attributes */
    val dividerAttributes: Map<String, Any?>? = null,
    /** Font name */
    val font: String? = null,
    /** Font color - hex string or color name from colors.json */
    val fontColor: String? = null,
    /** Font size */
    val fontSize: Double? = null,
    /** Hint text (alias for prompt) */
    val hint: String? = null,
    /** Hint text color - hex string or color name from colors.json (binding supported) */
    val hintColor: AttrValue<String>? = null,
    /** Display picker inside view */
    val inView: String? = null,
    /** Include prompt in binding */
    val includePromptWhenDataBinding: Boolean? = null,
    /** List of items for normal picker (can be template variable) */
    val items: AttrValue<List<Any?>>? = null,
    /** Label styling attributes */
    val labelAttributes: Map<String, Any?>? = null,
    /** Maximum date (yyyy-MM-dd format, binding supported) */
    val maximumDate: AttrValue<String>? = null,
    /** Minimum date (yyyy-MM-dd format, binding supported) */
    val minimumDate: AttrValue<String>? = null,
    /** Minute interval for time picker */
    val minuteInterval: Double? = null,
    /** Allow multiple selection */
    val multiple: Boolean? = null,
    /** Value change handler - binding only (@{functionName}) */
    val onValueChange: AttrValue<Any>? = null,
    /** Value change handler (alias for onValueChange) - binding only (@{functionName}) */
    val onValueChanged: AttrValue<Any>? = null,
    /** Placeholder text (alias for prompt) */
    val placeholder: String? = null,
    /** Prompt text (primary) */
    val prompt: String? = null,
    /** Reference view for picker */
    val referenceView: String? = null,
    /** Select item type */
    val selectItemType: AttrEnum<SelectItemType>? = null,
    /** Initial selected date (yyyy-MM-dd format, two-way binding) */
    val selectedDate: AttrValue<String>? = null,
    /** Initial selected index for normal picker (two-way binding) [binding: two-way] */
    val selectedIndex: AttrValue<Double>? = null,
    /** Selected item value (two-way binding) */
    val selectedItem: AttrValue<String>? = null,
    /** Selected value (binding for two-way) */
    val selectedValue: AttrValue<String>? = null,
    /** Number of visible options */
    val size: Double? = null,
    /** Display text (binding supported) */
    val text: AttrValue<String>? = null,
) {
    enum class ColorScheme(val json: String) {
        LIGHT("light"),
        DARK("dark");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): ColorScheme? = when (raw.lowercase()) {
                "light" -> LIGHT
                "dark" -> DARK
                else -> null
            }
        }
    }

    enum class DatePickerMode(val json: String) {
        DATE("date"),
        TIME("time"),
        DATETIME("datetime"),
        DATE_AND_TIME("dateAndTime"),
        COUNT_DOWN("countDown");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): DatePickerMode? = when (raw.lowercase()) {
                "date" -> DATE
                "time" -> TIME
                "datetime" -> DATETIME
                "dateandtime" -> DATE_AND_TIME
                "countdown" -> COUNT_DOWN
                else -> null
            }
        }
    }

    enum class DatePickerStyle(val json: String) {
        AUTOMATIC("automatic"),
        WHEEL("wheel"),
        WHEELS("wheels"),
        COMPACT("compact"),
        GRAPHICAL("graphical"),
        INLINE("inline");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): DatePickerStyle? = when (raw.lowercase()) {
                "automatic" -> AUTOMATIC
                "wheel" -> WHEEL
                "wheels" -> WHEELS
                "compact" -> COMPACT
                "graphical" -> GRAPHICAL
                "inline" -> INLINE
                else -> null
            }
        }
    }

    enum class SelectItemType(val json: String) {
        NORMAL("Normal"),
        DATE("Date");

        companion object {
            /** Case-insensitive match against the declared values. */
            fun from(raw: String): SelectItemType? = when (raw.lowercase()) {
                "normal" -> NORMAL
                "date" -> DATE
                else -> null
            }
        }
    }

    companion object {
        /**
         * Canonical attribute names declared for this component, including
         * the shared `common` set (public metadata contract).
         */
        val declaredAttributes: Set<String> = CommonAttributes.declaredAttributes + setOf(
            "canBack",
            "caretAttributes",
            "colorScheme",
            "datePickerMode",
            "datePickerStyle",
            "dateStringFormat",
            "dividerAttributes",
            "font",
            "fontColor",
            "fontSize",
            "hint",
            "hintColor",
            "inView",
            "includePromptWhenDataBinding",
            "items",
            "labelAttributes",
            "maximumDate",
            "minimumDate",
            "minuteInterval",
            "multiple",
            "onValueChange",
            "onValueChanged",
            "placeholder",
            "prompt",
            "referenceView",
            "selectItemType",
            "selectedDate",
            "selectedIndex",
            "selectedItem",
            "selectedValue",
            "size",
            "text",
        )

        /**
         * Alias spelling → canonical attribute name (merged with common).
         * Alias spellings that are also declared attributes keep
         * their own entry and are not redirected.
         */
        val aliasMap: Map<String, String> = emptyMap()

        /** True when `key` is a declared canonical name or alias spelling. */
        fun isDeclared(key: String): Boolean =
            key in declaredAttributes || key in aliasMap

        /**
         * Pass `canonicalOnly = true` for L1-normalized input —
         * alias fallback is then disabled.
         */
        fun parse(json: Map<String, Any?>, canonicalOnly: Boolean = false): SelectBoxAttributes = SelectBoxAttributes(
            common = CommonAttributes.parse(json, canonicalOnly),
            canBack = AttrCoerce.boolean(AttrCoerce.lookup(json, "canBack")),
            caretAttributes = AttrCoerce.obj(AttrCoerce.lookup(json, "caretAttributes")),
            colorScheme = parseColorScheme(AttrCoerce.lookup(json, "colorScheme")),
            datePickerMode = parseDatePickerMode(AttrCoerce.lookup(json, "datePickerMode")),
            datePickerStyle = parseDatePickerStyle(AttrCoerce.lookup(json, "datePickerStyle")),
            dateStringFormat = AttrCoerce.string(AttrCoerce.lookup(json, "dateStringFormat")),
            dividerAttributes = AttrCoerce.obj(AttrCoerce.lookup(json, "dividerAttributes")),
            font = AttrCoerce.string(AttrCoerce.lookup(json, "font")),
            fontColor = AttrCoerce.string(AttrCoerce.lookup(json, "fontColor")),
            fontSize = AttrCoerce.number(AttrCoerce.lookup(json, "fontSize")),
            hint = AttrCoerce.string(AttrCoerce.lookup(json, "hint")),
            hintColor = AttrCoerce.attrValue(AttrCoerce.lookup(json, "hintColor")) { AttrCoerce.string(it) },
            inView = AttrCoerce.string(AttrCoerce.lookup(json, "inView")),
            includePromptWhenDataBinding = AttrCoerce.boolean(AttrCoerce.lookup(json, "includePromptWhenDataBinding")),
            items = AttrCoerce.attrValue(AttrCoerce.lookup(json, "items")) { AttrCoerce.array(it) },
            labelAttributes = AttrCoerce.obj(AttrCoerce.lookup(json, "labelAttributes")),
            maximumDate = AttrCoerce.attrValue(AttrCoerce.lookup(json, "maximumDate")) { AttrCoerce.string(it) },
            minimumDate = AttrCoerce.attrValue(AttrCoerce.lookup(json, "minimumDate")) { AttrCoerce.string(it) },
            minuteInterval = AttrCoerce.number(AttrCoerce.lookup(json, "minuteInterval")),
            multiple = AttrCoerce.boolean(AttrCoerce.lookup(json, "multiple")),
            onValueChange = AttrCoerce.bindingValue(AttrCoerce.lookup(json, "onValueChange")),
            onValueChanged = AttrCoerce.bindingValue(AttrCoerce.lookup(json, "onValueChanged")),
            placeholder = AttrCoerce.string(AttrCoerce.lookup(json, "placeholder")),
            prompt = AttrCoerce.string(AttrCoerce.lookup(json, "prompt")),
            referenceView = AttrCoerce.string(AttrCoerce.lookup(json, "referenceView")),
            selectItemType = parseSelectItemType(AttrCoerce.lookup(json, "selectItemType")),
            selectedDate = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedDate")) { AttrCoerce.string(it) },
            selectedIndex = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedIndex")) { AttrCoerce.number(it) },
            selectedItem = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedItem")) { AttrCoerce.string(it) },
            selectedValue = AttrCoerce.attrValue(AttrCoerce.lookup(json, "selectedValue")) { AttrCoerce.string(it) },
            size = AttrCoerce.number(AttrCoerce.lookup(json, "size")),
            text = AttrCoerce.attrValue(AttrCoerce.lookup(json, "text")) { AttrCoerce.string(it) },
        )

        private fun parseColorScheme(raw: Any?): AttrEnum<ColorScheme>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                ColorScheme.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("SelectBox.colorScheme: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseDatePickerMode(raw: Any?): AttrEnum<DatePickerMode>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                DatePickerMode.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("SelectBox.datePickerMode: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseDatePickerStyle(raw: Any?): AttrEnum<DatePickerStyle>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                DatePickerStyle.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("SelectBox.datePickerStyle: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }

        private fun parseSelectItemType(raw: Any?): AttrEnum<SelectItemType>? {
            if (raw == null) return null
            (raw as? String)?.let { s ->
                SelectItemType.from(s)?.let { return AttrEnum.Known(it) }
            }
            AttrWarnings.emit("SelectBox.selectItemType: unknown enum value '$raw'")
            return AttrEnum.Unknown(raw)
        }
    }
}
